# คู่มือพรีเซนต์ Berserk Assemble - ครบทุกฟีเจอร์

---

## 🔐 ฟีเจอร์ที่ 1: ระบบ Login / Register (สมัครสมาชิกและเข้าสู่ระบบ)

### คืออะไร

ระบบสมัครสมาชิกและเข้าสู่ระบบ รองรับทั้ง Login (เข้าสู่ระบบ) และ Register (สมัครสมาชิกใหม่) ในหน้าเดียวกัน สลับโหมดได้ด้วยการกดปุ่ม มีระบบ Validation ตรวจสอบรหัสผ่านที่เข้มงวด (ต้องมีตัวพิมพ์ใหญ่, ตัวพิมพ์เล็ก, ตัวเลข, อักขระพิเศษ)

### ประโยชน์

ป้องกันการสร้างรหัสที่อ่อนแอ และใช้ JWT Token ในการยืนยันตัวตนทุกครั้งที่เรียก API หลังบ้าน ทำให้ระบบปลอดภัย

### วิธีแคป

- เปิดเว็บ → ไปที่หน้า `/login`
- แคป 1: หน้า Login (กรอก Username/Password)
- แคป 2: กดปุ่ม Register สลับเป็นหน้าสมัครสมาชิก (จะมีช่อง Display Name และ Confirm Password เพิ่มขึ้นมา)
- แคป 3: ลองกรอกรหัสผ่านผิด ดู Error Message ที่โชว์ทันที

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/login/login.ts` → Logic การ Login/Register และ Validation
- `client/src/app/login/login.html` → หน้าตา Form
- `client/src/app/_services/passport-service.ts` → จัดการ Token, เก็บข้อมูลผู้ใช้ลง LocalStorage
- `client/src/app/_helpers/password-validator.ts` → Validator รหัสผ่าน

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/authentication.rs` → API รับ Login/Register
- `server/src/application/use_cases/authentication.rs` → Logic ตรวจสอบ Username/Password + สร้าง JWT Token

### หลักการทำงาน

```typescript
// login.ts - เมื่อกด Submit จะเช็คว่าเป็น Login หรือ Register แล้วเรียก API ตาม mode
async onSubmit() {
  if (this.mode === 'login') {
    errMsg = await this._passport.get(this.form.value);     // เรียก POST /api/authentication/login
  } else {
    errMsg = await this._passport.register(this.form.value); // เรียก POST /api/brawler/register
  }
  if (!errMsg) this._router.navigate(['/']);  // Login สำเร็จ → กลับหน้าแรก
}
```

```typescript
// passport-service.ts - เมื่อ Login สำเร็จ เก็บ Token ลง LocalStorage เพื่อใช้ยืนยันตัวตนทุกหน้า
private async fetchPassport(api_url, model) {
  const passport = await firstValueFrom(this._http.post<Passport>(api_url, model));
  this.data.set(passport);                          // เก็บข้อมูลผู้ใช้ใน Signal
  this.avatar.set(getAvatarUrl(passport));           // ตั้งรูปโปรไฟล์ทันที
  this.savePassportToLocalStorage();                 // บันทึกลง LocalStorage
}
```

---

## 🏠 ฟีเจอร์ที่ 2: หน้าแรก (Home Page)

### คืออะไร

หน้าแรกที่ผู้ใช้เห็นหลัง Login แสดงข้อมูลสรุปเบื้องต้น (สมาชิกออนไลน์, คำขอเป็นเพื่อนที่รอดำเนินการ) และมีปุ่มทางลัดไปหน้าต่างๆ

### ประโยชน์

เป็น Landing Page ที่ให้ผู้ใช้เห็นสถานะ Community ทันทีหลัง Login (มีใครออนไลน์อยู่บ้าง, มีคำขอเป็นเพื่อนรออยู่กี่คน) พร้อมอัปเดตข้อมูลแบบ Real-time ผ่าน WebSocket

### วิธีแคป

- Login แล้ว → จะเข้าหน้า Home อัตโนมัติ (path: `/`)
- แคป 1: หน้า Home ทั้งหน้า (โชว์ Hero section, ปุ่ม Start Mission / Browse Crew)
- แคป 2: ส่วนที่แสดงจำนวนสมาชิกออนไลน์ (ถ้ามี)

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/home/home.ts` → Logic โหลดข้อมูล Online Users + Pending Requests
- `client/src/app/home/home.html` → หน้าตา
- `client/src/app/home/home.scss` → ดีไซน์ (Glassmorphism, Gradient)

### หลักการทำงาน

```typescript
// home.ts - โหลดข้อมูลจาก API หลายตัวพร้อมกันด้วย Promise.all
async loadData() {
  const [online, pending] = await Promise.all([
    this._friendship.getOnlineUsers(),      // ดึงรายการคนที่ออนไลน์
    this._friendship.getPendingRequests(),   // ดึงคำขอเป็นเพื่อนที่รออยู่
  ]);
  this.onlineUsers.set(online.filter(u => u.id !== myId)); // กรองตัวเองออก
  this.pendingRequestsCount.set(pending.length);
}

// Real-time: ถ้ามีคนเข้า/ออกระบบ จะโหลดข้อมูลใหม่ทันที
setupRealtime() {
  this._wsService.notifications$.subscribe((msg) => {
    if (['agent_online', 'agent_offline', 'friend_request'].includes(msg.type)) {
      this.loadData();  // ข้อมูลอัปเดตทันทีโดยไม่ต้อง Refresh
    }
  });
}
```

---

## 📋 ฟีเจอร์ที่ 3: ดูกิจกรรมทั้งหมด (Explore Missions)

### คืออะไร

หน้ารวบรวมกิจกรรมทั้งหมดในระบบที่เปิดให้เข้าร่วมได้ สามารถค้นหาด้วยชื่อ, กรองตามหมวดหมู่ (Sports, Social, Gaming, Travel ฯลฯ), กรองตามสถานะ (Open, In Progress) และกดปุ่ม Join เพื่อเข้าร่วมทีมได้ทันที

### ประโยชน์

ผู้ใช้สามารถค้นหากิจกรรมที่ตรงกับความสนใจและเข้าร่วมได้เลย มีระบบ Onboarding Guide สำหรับผู้ใช้ใหม่ที่จะสอนใช้งานหน้านี้โดยอัตโนมัติ

### วิธีแคป

- กดเมนู Missions (Navbar) → ไปที่ `/missions`
- แคป 1: หน้ารวมกิจกรรมทั้งหมด (มีช่อง Search, Dropdown หมวดหมู่)
- แคป 2: ลองกรองหมวดหมู่ เช่น "Sports & Active" ให้เห็นว่ากรองได้จริง
- แคป 3: กดปุ่ม Join Mission อันใดอันหนึ่ง
- แคป 4: ถ้าเข้าครั้งแรก จะมี Onboarding Guide (หน้าจอแนะนำ) โผล่ขึ้นมา

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/missions/missions.ts` → Logic ค้นหา, กรอง, Join
- `client/src/app/missions/missions.html` → หน้าตาการ์ดกิจกรรม
- `client/src/app/_services/mission-service.ts` → Service เรียก API ภารกิจ
- `client/src/app/_services/crew-service.ts` → Service เรียก API Join/Leave
- `client/src/app/_components/guide-overlay/guide-overlay.ts` → ระบบ Onboarding
- `client/src/app/_services/onboarding-service.ts` → เช็คว่าผู้ใช้เคยดู Guide หรือยัง

### ไฟล์โค้ดหลังบ้าน

- `server/src/application/use_cases/mission_viewing.rs` → API ค้นหาภารกิจตาม Filter
- `server/src/application/use_cases/crew_operation.rs` → API Join/Leave ทีม

### หลักการทำงาน

```typescript
// missions.ts - ระบบกรองหมวดหมู่กิจกรรม
categories = ['Sports & Active', 'Social & Chill', 'Gaming & E-Sports',
              'Entertainment', 'Travel & Trip', 'Lifestyle & Hobby', 'Other'];

async onSubmit() {
  this.filter.exclude_user_id = userId;  // ไม่โชว์กิจกรรมที่ตัวเองสร้างหรือเข้าร่วมแล้ว
  const missions = await this._mission.getByFilter(this.filter); // ส่ง Filter ไปยัง Backend
  this._missionsSubject.next(missions);
}

// กดปุ่ม Join
async onJoin(mission) {
  await this._crewService.join(mission.id);  // เรียก API POST /api/crew/join/:id
  this.onSubmit();                           // โหลดรายการใหม่ (กิจกรรมที่ Join แล้วจะหายไป)
}
```

---

## 📝 ฟีเจอร์ที่ 4: รายละเอียดกิจกรรม (Mission Detail)

### คืออะไร

หน้าแสดงรายละเอียดกิจกรรมเต็มรูปแบบ รวมถึงดูสมาชิกในทีม (Crew), ระบบ Discussion (พิมพ์คอมเมนต์คุยกัน), และ Chief (เจ้าของ) สามารถจัดการสถานะภารกิจได้ (Start, Complete, Fail) รวมถึงการ Kick สมาชิกออก

### ประโยชน์

ศูนย์กลางของแต่ละกิจกรรม ที่ทุกคนสามารถเห็นข้อมูล, คุยกัน, และเปลี่ยนสถานะ ทุกอย่างอัปเดตแบบ Real-time ด้วย WebSocket

### วิธีแคป

- จากหน้า Missions → กดที่กิจกรรมใดก็ได้ → เข้าหน้า `/missions/:id`
- แคป 1: หน้า Detail ทั้งหน้า (ข้อมูลกิจกรรม, สมาชิกทีม, ปุ่มดำเนินการ)
- แคป 2: ส่วน Discussion (ลองพิมพ์ข้อความ แล้วกด Send)
- แคป 3: (ถ้าเป็น Chief) โชว์ปุ่ม Start / Complete / Fail / Kick

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/missions/mission-detail/mission-detail.ts` → Logic ทั้งหมดของหน้า Detail
- `client/src/app/missions/mission-detail/mission-detail.html` → หน้าตา

### ไฟล์โค้ดหลังบ้าน

- `server/src/application/use_cases/mission_operation.rs` → API เปลี่ยนสถานะ (Start/Complete/Fail)
- `server/src/application/use_cases/mission_comment.rs` → API คอมเมนต์ในภารกิจ
- `server/src/application/use_cases/crew_operation.rs` → API Join/Leave/Kick

### หลักการทำงาน

```typescript
// mission-detail.ts - WebSocket รับข้อมูลอัปเดตแบบ Real-time
handleWsMessage(msg) {
  switch (msg.type) {
    case 'new_comment':       // มีคนพิมพ์คอมเมนต์ใหม่ → เพิ่มลงหน้าจอทันที
    case 'new_crew_joined':   // มีคนเข้าทีม → โหลดข้อมูลใหม่
    case 'mission_started':   // Chief กดเริ่ม → สถานะเปลี่ยนทันที
    case 'kicked_from_mission': // ถูก Kick → Navigate ออกจากหน้านี้
      this.loadMission(this.missionId);  // โหลดข้อมูลใหม่ทันที
  }
}

// ส่งคอมเมนต์: ส่งผ่าน WebSocket ไม่ต้อง Refresh หน้า
sendComment() {
  this._wsService.sendMessage('comment', { mission_id: this.missionId, content: text });
}
```

---

## ⚙️ ฟีเจอร์ที่ 5: จัดการกิจกรรมที่ตัวเองสร้าง (Mission Manager / Chief)

### คืออะไร

หน้าสำหรับ "Chief" (คนสร้างกิจกรรม) จัดการกิจกรรมทั้งหมดที่ตัวเองเป็นเจ้าของ สร้างใหม่, แก้ไข, ลบ, ดูสมาชิกทีม, เปลี่ยนสถานะ (Start/Complete/Fail), Kick สมาชิกออก

### ประโยชน์

เครื่องมือจัดการสำหรับเจ้าของกิจกรรม ทุกอย่างอยู่ในที่เดียว ข้อมูลอัปเดต Real-time

### วิธีแคป

- กดเมนู Chief (Navbar) → ไปที่ `/chief`
- แคป 1: หน้ารวมกิจกรรมที่ตัวเองสร้าง
- แคป 2: กดปุ่ม "+" (สร้างกิจกรรมใหม่) → จะมี Dialog ขึ้นมาให้กรอกข้อมูล
- แคป 3: โชว์ปุ่ม Edit, Delete, Start, Complete สำหรับแต่ละกิจกรรม
- แคป 4: กดดูสมาชิกทีม (Crew) ในแต่ละกิจกรรม

### ไฟล์โค้ดหน้าบ้าน
z
- `client/src/app/missions/mission-manager/mission-manager.ts` → Logic จัดการกิจกรรมทั้งหมด
- `client/src/app/missions/mission-manager/mission-manager.html` → หน้าตา
- `client/src/app/_dialogs/new-mission/new-mission.ts` → Dialog สร้าง/แก้ไขกิจกรรม

### ไฟล์โค้ดหลังบ้าน

- `server/src/application/use_cases/mission_management.rs` → API สร้าง/แก้ไข/ลบกิจกรรม

### หลักการทำงาน

```typescript
// mission-manager.ts - สร้างกิจกรรมใหม่ผ่าน Dialog
openDialog() {
  const ref = this._dialog.open(NewMission);          // เปิด Dialog
  ref.afterClosed().subscribe(async (result) => {
    if (result) {
      await this._missionService.add(result);          // เรียก API POST สร้างกิจกรรม
      this._toast.success('Mission created successfully!');
      this.loadMyMission();                            // โหลดรายการใหม่
    }
  });
}

// Kick สมาชิกออกจากทีม
async onKick(mission, member) {
  await this._missionService.kickMember(mission.id, member.id);
  this.loadMyMission();   // อัปเดตหน้าจอ
}
```

---

## 👥 ฟีเจอร์ที่ 6: กิจกรรมที่ฉันเข้าร่วม (My Crew)

### คืออะไร

หน้าดูกิจกรรมทั้งหมดที่ตัวเราเข้าร่วมเป็น Crew (ไม่ใช่ Chief) สามารถกด Leave (ออกจากทีม) ได้

### ประโยชน์

ผู้ใช้เห็นภาพรวมว่าตัวเองเข้าร่วมกิจกรรมไหนอยู่บ้าง จัดการเวลาได้ง่าย

### วิธีแคป

- กดเมนู My Crew (Navbar) → ไปที่ `/my-crew`
- แคป 1: รายการกิจกรรมที่เข้าร่วม
- แคป 2: กดปุ่ม Leave Mission ที่กิจกรรมใดก็ได้

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/my-crew/my-crew.ts` → Logic โหลดกิจกรรมที่เข้าร่วม + Leave

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/crew_operation.rs` → API Leave

### หลักการทำงาน

```typescript
// my-crew.ts - ออกจากทีม
async onLeave(mission, event) {
  await this._crewService.leave(mission.id);      // เรียก API DELETE /api/crew/leave/:id
  this._toast.success(`You have left "${mission.name}".`);
  this.loadMyJoinedMissions();                     // อัปเดตรายการทันที
}
```

---

## 📊 ฟีเจอร์ที่ 7: แดชบอร์ดวิเคราะห์กิจกรรม (Dashboard Analytics)

### คืออะไร

หน้าสรุปสถิติทั้งหมดของตัวเอง (รวมกิจกรรมที่สร้าง + ที่เข้าร่วม) โชว์ตัวเลข Open, In Progress, Completed, Failed, Success Rate, Avg Crew Size, Total XP พร้อมรายการกิจกรรมล่าสุด, สมาชิกออนไลน์, และคำขอเป็นเพื่อน

### ประโยชน์

เห็นสุขภาพของ Community ในภาพรวม และจัดการคำขอเป็นเพื่อนได้โดยไม่ต้องไปหน้า Network

### วิธีแคป

- กดเมนู Dashboard (Navbar) → ไปที่ `/dashboard`
- แคป 1: ส่วน Stats Card (Total, Success Rate, In Progress, XP)
- แคป 2: รายการ Recent Missions
- แคป 3: ส่วนคนออนไลน์ และ Pending Friend Requests

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/dashboard/dashboard.ts` → Logic คำนวณสถิติ
- `client/src/app/dashboard/dashboard.html` → หน้าตา
- `client/src/app/dashboard/dashboard.scss` → ดีไซน์การ์ด

### ไฟล์โค้ดหลังบ้าน

- ใช้ API เดิมจาก `mission-service.ts` และ `crew-service.ts`

### หลักการทำงาน

```typescript
// dashboard.ts - รวมกิจกรรมจาก 2 แหล่ง (สร้างเอง + เข้าร่วม) แล้วคำนวณสถิติ
async loadStats() {
  const myMissions = await this._missionService.getMyMissions();     // กิจกรรมที่สร้าง
  const joinedMissions = await this._crewService.getMyJoinedMissions(); // กิจกรรมที่เข้าร่วม
  const allMissions = [...myMissions, ...joinedMissions];
  // กรองไม่ให้ซ้ำ
  const uniqueMissions = Array.from(new Map(allMissions.map(item => [item.id, item])).values());
  this.calculateStats(uniqueMissions);
}

// คำนวณ Success Rate
private calculateStats(missions) {
  const completedCount = missions.filter(m => m.status === 'Completed').length;
  const failedCount = missions.filter(m => m.status === 'Failed').length;
  this.stats.successRate = Math.round((completedCount / (completedCount + failedCount)) * 100);
  this.stats.totalExp = completedCount * 100;  // XP = 100 ต่อกิจกรรมที่สำเร็จ
}
```

---

## 🌐 ฟีเจอร์ที่ 8: ระบบเพื่อนและเครือข่าย (Network / Members)

### คืออะไร

หน้าจัดการเพื่อนทั้งหมด แบ่งเป็น 3 แท็บ: All Friends (เพื่อนทั้งหมด), Requests (คำขอเป็นเพื่อนที่รอ), Online (คนที่กำลังออนไลน์อยู่) Accept/Reject คำขอเป็นเพื่อน, ลบเพื่อน, เปิดแชทส่วนตัวได้

### ประโยชน์

ศูนย์กลางจัดการความสัมพันธ์ เช็คว่าใครออนไลน์เพื่อนัดทำกิจกรรมด้วยกัน

### วิธีแคป

- กดเมนู Members (Navbar) → ไปที่ `/network`
- แคป 1: แท็บ All (รายชื่อเพื่อนทั้งหมด)
- แคป 2: แท็บ Requests (คำขอเป็นเพื่อนที่รอ พร้อมปุ่ม Accept/Reject)
- แคป 3: แท็บ Online (คนที่มีจุดเขียวบอกว่าออนไลน์อยู่)

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/network/network.ts` → Logic แท็บ, Accept, Reject, Remove
- `client/src/app/network/network.html` → หน้าตา
- `client/src/app/_services/friendship-service.ts` → Service เรียก API Friendship

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/friendships.rs` → API เพื่อน (Send/Accept/Reject/Remove)
- `server/src/application/use_cases/friendships.rs` → Logic ตรวจสอบระบบเพื่อน

### หลักการทำงาน

```typescript
// network.ts - การ Accept คำขอเป็นเพื่อน
async onAccept(id: number) {
  await this._friendship.acceptRequest(id);   // เรียก API PATCH /api/friendship/accept/:id
  this.refreshAll();                           // โหลดข้อมูลทุกแท็บใหม่
}

// Network หน้านี้โหลดข้อมูล 3 อย่างพร้อมกัน
async refreshAll() {
  await Promise.all([this.loadFriends(), this.loadPendingRequests(), this.loadOnlineUsers()]);
}
```

---

## 💬 ฟีเจอร์ที่ 9: ระบบแชทส่วนตัว (Private Chat)

### คืออะไร

ระบบแชทส่วนตัวที่อยู่มุมขวาล่างของจอ (แบบ Facebook Messenger) ค้นหาเพื่อนที่จะแชทด้วยได้, ดูแชทล่าสุด, ส่งข้อความแบบเรียลไทม์ผ่าน WebSocket, มีจำนวนข้อความที่ยังไม่อ่าน (Unread Badge) แสดงที่ไอคอน

### ประโยชน์

ผู้ใช้สามารถคุยนัดแนะรายละเอียดกิจกรรมกันได้โดยตรง ใช้งานได้ทุกหน้าเว็บ (เป็น Component ที่ฝังอยู่ทุกหน้า)

### วิธีแคป

- กดปุ่มไอคอนแชท (มุมขวาล่าง) → แผ่นแชทจะเด้งขึ้นมา
- แคป 1: แผ่นแชท (Recent Chats)
- แคป 2: ลองพิมพ์ชื่อเพื่อนในช่อง Search → เลือกเพื่อนเพื่อเริ่มแชทใหม่
- แคป 3: พิมพ์ข้อความแล้วกด Send → ข้อความขึ้นทันทีแบบเรียลไทม์
- แคป 4: ปิดแชท → จะเห็นตัวเลข Unread Badge ที่ไอคอนแชท (ถ้ามีข้อความที่ยังไม่อ่าน)

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/_components/private-chat/private-chat.ts` → Logic แชท, ค้นหา, ส่งข้อความ
- `client/src/app/_components/private-chat/private-chat.html` → หน้าตา Widget
- `client/src/app/_services/chat-service.ts` → Service เรียก API แชท + WebSocket

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/private_messages.rs` → API ส่ง/อ่านข้อความ
- `server/src/infrastructure/database/repositories/private_messages.rs` → SQL Query

### หลักการทำงาน

```typescript
// private-chat.ts - ส่งข้อความแบบ Optimistic UI (แสดงทันทีก่อน Server ตอบ)
send() {
  // 1. แสดงข้อความบนจอทันที (ยังไม่ได้ไป Server)
  const tempMsg = { id: -1, content: this.newMessage, sender_id: this.currentUserId };
  this.messages.push(tempMsg);
  this.scrollToBottom();

  // 2. ส่งไป Server จริง
  this._chatService.sendMessage(this.selectedUser.id, payload).subscribe((msg) => {
    this.messages = this.messages.filter(m => m.id !== -1); // ลบ temp
    this.messages.push(msg);                                // ใส่ข้อความจริงที่ Server ส่งกลับ
  });
}

// รับข้อความ Real-time จาก WebSocket
this._chatService.incomingMessage$.subscribe((msg) => {
  if (msg.sender_id === this.selectedUser.id) {
    this.messages.push(msg);     // แสดงทันทีบนจอ
    this.scrollToBottom();
  } else {
    this.updateUnreadCount();    // เพิ่มจำนวน Unread Badge
  }
});
```

---

## 🔔 ฟีเจอร์ที่ 10: ระบบแจ้งเตือน (Notification Bell)

### คืออะไร

ไอคอนกระดิ่งที่อยู่ใน Navbar แสดงจำนวนการแจ้งเตือนที่ยังไม่อ่าน กดดูรายการแจ้งเตือนทั้งหมดได้ (คำขอเป็นเพื่อน, มีคนเข้าทีม, กิจกรรมเริ่มแล้ว ฯลฯ) สามารถ Accept คำขอเป็นเพื่อนได้ตรงจากตรงนี้เลย

### ประโยชน์

ผู้ใช้ไม่พลาดเหตุการณ์สำคัญ มีการแจ้งเตือนแบบ Real-time ผ่าน WebSocket

### วิธีแคป

- กดไอคอนกระดิ่ง (Navbar ขวาบน) → จะมี Dropdown รายการแจ้งเตือน
- แคป 1: Dropdown ที่แสดงรายการแจ้งเตือน
- แคป 2: Badge ตัวเลขที่ไอคอนกระดิ่ง (ถ้ามีแจ้งเตือนที่ยังไม่อ่าน)

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/_components/notification-bell/notification-bell.ts` → Logic กระดิ่ง + Accept Friend
- `client/src/app/_services/notification-service.ts` → Service เรียก API Notification + WebSocket

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/notifications.rs` → API Notification
- `server/src/application/use_cases/notifications.rs` → Logic Notification

### หลักการทำงาน

```typescript
// notification-bell.ts - กด Accept คำขอเป็นเพื่อนจากกระดิ่ง
async acceptFriendRequest(notif) {
  const pending = await this.friendshipService.getPendingRequests();
  const req = pending.find(r => r.requester_id === notif.related_id);
  if (req) {
    await this.friendshipService.acceptRequest(req.id);   // Accept
    this.notificationService.markAsRead(notif.id);        // ทำเครื่องหมายว่าอ่านแล้ว
  }
}

// ไอคอนเปลี่ยนตามประเภทแจ้งเตือน
getNotificationIcon(type) {
  switch (type) {
    case 'friend_request': return 'person_add';
    case 'mission_started': return 'play_circle';
    case 'new_crew_joined': return 'person_add';
    case 'kicked_from_mission': return 'person_remove';
  }
}
```

---

## 👤 ฟีเจอร์ที่ 11: โปรไฟล์ส่วนตัว (Profile & Edit Profile)

### คืออะไร

หน้าโปรไฟล์ที่โชว์ข้อมูลส่วนตัว (ชื่อ, รูป Avatar, Bio), ข้อมูลติดต่อ (Discord, Instagram, Facebook, Email), สถิติ (กิจกรรมที่สร้าง, กิจกรรมที่เข้าร่วม, กิจกรรมที่กำลังทำ), และ Rank (Newcomer / Member / Organizer) ดูโปรไฟล์คนอื่นได้ + กดแอดเพื่อน + กดแชทได้จากหน้านี้

### ประโยชน์

สร้างตัวตนดิจิทัลและเพิ่มความน่าเชื่อถือ ยิ่งทำกิจกรรมมาก Rank ยิ่งสูง

### วิธีแคป

- กดที่รูป Avatar (Navbar ขวาบน) → ไปที่ `/profile`
- แคป 1: โปรไฟล์ตัวเอง (ชื่อ, bio, ข้อมูลติดต่อ, สถิติ, Rank)
- แคป 2: กดปุ่ม Edit Profile → จะมี Dialog ขึ้นมาให้แก้ไขข้อมูล
- แคป 3: กดปุ่มเปลี่ยนรูปโปรไฟล์ → Dialog Upload รูป
- แคป 4: เข้าดูโปรไฟล์คนอื่น (เช่น จากหน้า Network กดชื่อเพื่อน) → จะเห็นปุ่ม Add Friend / Chat

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/profile/profile.ts` → Logic โปรไฟล์, สถิติ, Rank, Add Friend
- `client/src/app/profile/profile.html` → หน้าตา
- `client/src/app/profile/edit-profile-dialog.ts` → Dialog แก้ไขข้อมูล
- `client/src/app/_dialogs/upload-img/upload-img.ts` → Dialog อัปโหลดรูป

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/brawlers.rs` → API ข้อมูลผู้ใช้ + อัปเดตโปรไฟล์

### หลักการทำงาน

```typescript
// profile.ts - คำนวณสถิติอัตโนมัติจากข้อมูลกิจกรรม
stats = computed(() => {
  const all = this.missions();
  const hosted = all.filter((m) => m.chief_id === profileUserId).length; // กิจกรรมที่สร้างเอง
  const joined = all.filter((m) => m.chief_id !== profileUserId).length; // กิจกรรมที่เข้าร่วม
  const active = all.filter((m) =>
    ["Open", "In Progress"].includes(m.status),
  ).length;
  return { hosted, joined, active };
});

// ระบบ Rank อัตโนมัติ
rank = computed(() => {
  const total = this.missions().length;
  if (total > 20) return "Organizer"; // ทำกิจกรรมมากกว่า 20 ครั้ง
  if (total > 5) return "Member"; // ทำกิจกรรมมากกว่า 5 ครั้ง
  return "Newcomer"; // มือใหม่
});
```

---

## ⚡ ฟีเจอร์ที่ 12: ระบบ Real-time ทั้งเว็บ (WebSocket)

### คืออะไร

ระบบ WebSocket ที่เชื่อมต่อทุกหน้าเว็บเข้าด้วยกัน ทำให้ทุกการเปลี่ยนแปลง (มีคนเข้าทีม, กิจกรรมเริ่ม, มีข้อความใหม่, มีคนออนไลน์) อัปเดตไปยังทุกคนที่กำลังเปิดเว็บอยู่ทันที โดยไม่ต้อง Refresh

### ประโยชน์

ผู้ใช้รู้สึกว่าเว็บ "มีชีวิต" ข้อมูลอัปเดตอยู่ตลอดเวลา ไม่ต้องกด Refresh ทำให้การนัดหมายกันราบรื่นมากขึ้น

### วิธีแคป

- เปิดเว็บ 2 หน้าต่าง (คนละ Account) → ให้คนหนึ่งกด Join Mission → อีกฝั่งจะเห็นจำนวน Crew เปลี่ยนทันที
- แคป 1: หน้าจอ 2 ฝั่งเทียบกัน (Before/After)
- แคป 2: กดส่งข้อความแชท → อีกฝั่งจะเห็นข้อความขึ้นทันที

### ไฟล์โค้ดหน้าบ้าน

- `client/src/app/_services/websocket-service.ts` → Service จัดการ WebSocket ทั้ง 2 ช่อง (Mission + Global Notification)

### ไฟล์โค้ดหลังบ้าน

- `server/src/infrastructure/http/routers/` → WebSocket handlers ฝั่ง Server (Rust/Axum)

### หลักการทำงาน

```typescript
// websocket-service.ts - เชื่อมต่อ WebSocket สำหรับ Notification ทั้งระบบ
connectNotifications() {
  const url = `ws://localhost:8000/api/ws/global?token=${token}`;
  this.notificationSocket = new WebSocket(url);

  this.notificationSocket.onmessage = (event) => {
    this._ngZone.run(() => {              // บังคับให้ Angular รับรู้การเปลี่ยนแปลง
      const data = JSON.parse(event.data);
      this.notificationSubject.next(data); // ส่งข้อมูลไปยังทุก Component ที่ Subscribe อยู่
    });
  };
}
```

---

## 🧭 ฟีเจอร์ที่ 13: Navbar (แถบเมนูหลัก)

### คืออะไร

แถบเมนูด้านบนที่มีทุกหน้าเว็บ แสดงลิงก์ไปหน้าต่างๆ (Home, Missions, Chief, My Crew, Dashboard, Members) พร้อมโชว์ชื่อผู้ใช้ + รูป Avatar + กระดิ่ง Notification + ไอคอนคนออนไลน์ + ปุ่ม Logout

### วิธีแคป

- แคปแถบ Navbar ทั้งแถบ (โชว์ชื่อผู้ใช้, Avatar, กระดิ่ง)

### ไฟล์โค้ด

- `client/src/app/navbar/navbar.ts` → Logic Navbar + Logout
- `client/src/app/navbar/navbar.html` → หน้าตา

### หลักการทำงาน

```typescript
// navbar.ts - แสดงชื่อและรูปผู้ใช้ที่ Login อยู่
display_name = computed(() => this._passport.data()?.display_name); // ดึงชื่อจาก Signal
avatar_url = computed(() => this._passport.avatar());                // ดึงรูปจาก Signal

logout() {
  this._passport.destroy();              // ลบ Token ออกจาก LocalStorage
  this._router.navigate(['/login']);      // พาไปหน้า Login
}
```

---

## 🎨 ดีไซน์ภาพรวม (UX/UI)

### สไตล์หลัก

- **โทนสี:** Dark Mode + สีม่วง/ชมพู Mauve + Glassmorphism (การ์ดโปร่งแสง)
- **Typography:** ใช้ฟอนต์สะอาดอ่านง่าย
- **Layout:** ระบบ Grid + Flexbox รองรับทุกขนาดหน้าจอ
- **Animation:** Hover Effects, Glow Buttons, Smooth Transitions

### ไฟล์ CSS หลัก

- `client/src/app/home/home.scss` → ดีไซน์หน้าแรก
- `client/src/app/dashboard/dashboard.scss` → ดีไซน์ Dashboard
- `client/src/app/profile/profile.scss` → ดีไซน์ Profile
- `client/src/app/missions/missions.scss` → ดีไซน์หน้ากิจกรรม

---

## 🏗️ โครงสร้างทางเทคนิค (Architecture)

### Frontend: Angular 19 + Signals

- ใช้ **Angular Signals** แทน RxJS ในหลายจุด เพื่อลดความซับซ้อน (เช่น `signal()`, `computed()`)
- ใช้ **Standalone Components** ไม่ต้องมี NgModule
- ใช้ **Angular Material** สำหรับ UI Components (Button, Icon, Dialog, Menu)

### Backend: Rust / Axum

- ใช้โครงสร้าง **Clean Architecture** แยกชั้นชัดเจน:
  - `use_cases/` → Business Logic
  - `repositories/` → SQL Query ไปยัง Database
  - `routers/` → HTTP API Endpoints
- **WebSocket** สำหรับ Real-time (Mission Chat + Global Notification)
- **JWT Token** สำหรับ Authentication

### Database: PostgreSQL

- ตาราง: `brawlers` (ผู้ใช้), `missions`, `crews`, `comments`, `friendships`, `private_messages`, `notifications`
